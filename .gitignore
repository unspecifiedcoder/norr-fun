node_modules
.env

# Hardhat files
/cache
/artifacts

# TypeChain files
/typechain
/typechain-types

# solidity-coverage files
/coverage
/coverage.json

# Hardhat Ignition default folder for deployments against a local node
ignition/deployments/chain-31337
/zkit
temp-register-proof.json
temp-register-inputs.json
/deployments
/generated-types

# Key material. These files hold eERC decryption keys in plaintext; three of
# them were previously committed and published. Nothing in the app reads them
# -- they are script output -- so they are ignored rather than relocated.
**/user-keys*.json
*.keys.json
