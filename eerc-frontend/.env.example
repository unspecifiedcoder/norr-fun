# Optional. A WalletConnect / Reown project id from https://cloud.reown.com.
# Without it the injected (MetaMask, Core, Rabby) and Coinbase connectors still
# work; the WalletConnect option is simply not offered.
VITE_WALLETCONNECT_PROJECT_ID=
